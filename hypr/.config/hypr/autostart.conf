# Disable internal display at startup if external is connected
exec-once = bash ~/.config/hypr/scripts/toggle-internal-display.sh

# Clipboard manager (for Wayland clipboard support)
exec-once = wl-paste --watch cliphist store

# Re-apply fullscreen for native games that reset their window mode after init
# (script kept for future use, uncomment if needed)
# exec-once = bash ~/.config/hypr/scripts/fullscreen-game.sh
