{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "commit": {
      "description": "Create a git commit following project conventions",
      "template": "Load the commit skill and follow its instructions. Group changes by top-level component. Create one commit per component unless tightly coupled.",
      "subtask": true
    },
    "stow-deploy": {
      "description": "Deploy, list, or preview stow packages",
      "template": "Load the stow-deploy skill and follow its instructions. If an argument is given, use it as the package name or action (list/preview).",
      "subtask": true
    },
    "handoff": {
      "description": "Write a handoff document for interrupted work",
      "template": "Load the handoff skill and document the current session's discoveries, decisions, and remaining work so another agent can continue.",
      "subtask": true
    },
    "summary-handoffs": {
      "description": "Archive handoffs into a single summary document",
      "template": "Load the summary-handoffs skill. Read existing handoffs and related files, produce a concise summary, and archive the originals.",
      "subtask": true
    }
  },
  "provider": {
    "ollama": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Ollama (local)",
      "options": {
        "baseURL": "http://localhost:11434/v1"
      },
      "models": {
        "qwen3.5:9b-q4_K_M": {
          "name": "Qwen 3.5 9B Q4"
        },
        "qwen3:4b-instruct": {
          "name": "Qwen 3 4B Instruct"
        }
      }
    }
  }
}
